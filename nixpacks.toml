[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = [
  "cd server && npm install",
  "cd client && npm install && npx vite build",
  "cd server && npx tsc"
]

[start]
cmd = "cd server && node dist/index.js"